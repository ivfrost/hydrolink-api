server.port=8080
spring.main.banner-mode=off

# Device secrets
device.key-secret=${DEVICE_KEY_SECRET}
device.provisioning-secret=${DEVICE_PROVISIONING_SECRET}

# --- OAuth2 Cognito ---
aws.user.pool.id=${AWS_USER_POOL_ID}
aws.identity.pool.id=${AWS_IDENTITY_POOL_ID}
aws.account.id=${AWS_ACCOUNT_ID}
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://cognito-idp.${AWS_REGION}.amazonaws.com/${AWS_USER_POOL_ID}
spring.security.oauth2.resourceserver.jwt.audiences=${AWS_USER_POOL_CLIENT_ID}

# --- Database ---
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.show-sql=false
spring.sql.init.mode=never
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.defer-datasource-initialization=false
spring.flyway.enabled=true

# --- AWS (device status queue + data plane publish) ---
# Prod resolves credentials from the deployment role, so no static keys here.
spring.cloud.aws.region.static=${AWS_REGION}
events.queues.device-status-queue=${DEVICE_STATUS_QUEUE}

# --- Redis ---
# ElastiCache Serverless requires TLS on the wire, so ssl must be true.
# This cache has no user group (no auth token), so REDIS_PASSWORD must be empty:
# sending AUTH to a cache with no auth configured is rejected and the connection
# fails, which marks the redis health indicator DOWN.
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.ssl.enabled=${REDIS_SSL_ENABLED}
spring.cache.redis.time-to-live=10m

# --- Actuator health ---
# The ALB and the ECS container health check both poll the health endpoint and both
# require a 2xx. probes.enabled exposes the liveness/readiness groups outside
# Kubernetes; add-additional-paths serves them on the main server port (8080) as
# well as the management port, which is what the container health check needs.
management.endpoint.health.probes.enabled=true
management.endpoint.health.probes.add-additional-paths=true
management.endpoint.health.show-details=always

# --- Storage (MinIO - S3) ---
minio.url=${MINIO_URL}
minio.ext-url=${MINIO_EXT_URL}
minio.bucket-name=${MINIO_DEFAULT_BUCKETS}
minio.bucket-region=${MINIO_BUCKET_REGION}

# --- Hibernate ---
spring.autoconfigure.exclude=\
  org.springframework.modulith.runtime.autoconfigure.SpringModulithRuntimeAutoConfiguration,\
  org.springframework.modulith.actuator.autoconfigure.ApplicationModulesEndpointConfiguration
spring.jpa.properties.hibernate.physical_naming_strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
spring.jpa.properties.hibernate.implicit_naming_strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl

# --- Modulith ---
spring.modulith.runtime.verification-enabled=false
spring.modulith.events.republish-outstanding-events-on-restart=true
spring.modulith.events.jdbc.schema-initialization.enabled=true

# --- Proxy / Reverse proxy awareness ---
server.forward-headers-strategy=framework

# --- CORS ---
cors.allowed-origins=${CORS_ALLOWED_ORIGINS}

# --- SwaggerUI (disabled in prod) ---
springdoc.api-docs.enabled=${SPRINGDOC_API_DOCS_ENABLED}
springdoc.swagger-ui.enabled=${SPRINGDOC_SWAGGER_UI_ENABLED}

# --- Logging ---
logging.level.org.springframework=ERROR
logging.level.org.springframework.web=WARN
logging.level.dev.ivfrost.hydro_backend=INFO
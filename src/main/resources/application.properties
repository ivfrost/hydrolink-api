spring.config.import=optional:file:../.env[.properties],optional:file:.env[.properties]
# SwaggerUI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/api-docs-ui
springdoc.swagger-ui.operationsSorter=method
# Serve only the grouped specs (public + internal). The default ungrouped spec would otherwise
# still expose the internal-only /v1/internal endpoints to API consumers.
springdoc.enable-default-api-docs=false
# Use CGLIB proxies to allow event listener methods on target classes
spring.aop.proxy-target-class=true
# Allow firmware (.bin) uploads — ESP32 OTA images routinely exceed the 1MB Spring default
spring.servlet.multipart.max-file-size=8MB
spring.servlet.multipart.max-request-size=10MB
# Hibernate
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
# Docker
spring.docker.compose.enabled=false